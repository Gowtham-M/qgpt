server:
  env_name: test
  auth:
    enabled: false
    # Dummy secrets used for tests
    secret: "foo bar; dummy secret"

data:
  everi_ai_qgpt_vectordb_qdrant_folder: everi_ai_qgpt_vectordb_qdrant/tests

qdrant:
  path: everi_ai_qgpt_vectordb_qdrant/tests

llm:
  mode: mock

embedding:
  mode: mock

ui:
  enabled: false