server:
  env_name: test
  auth:
    enabled: false
    # Dummy secrets used for tests
    secret: "foo bar; dummy secret"

data:
  qgpt_vectordb_qdrant_folder: qgpt_vectordb_qdrant/tests

qdrant:
  path: qgpt_vectordb_qdrant/tests

llm:
  mode: mock

embedding:
  mode: mock

ui:
  enabled: false